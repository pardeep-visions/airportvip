<?php


add_image_size( 'hd', 1920, 3800 ); // Adds HD Width Image, soft proportional crop mode

// add_filter( 'big_image_size_threshold', '__return_false' ); // Prevents WP from scaling max image size
